version: "3.9"
services:
    app:
        build:
            context: ./app
            dockerfile: Dockerfile
        ports:
            - "5173:5173"
        volumes:
            - ./app:/usr/src/app
            - /usr/src/app/node_modules
        environment:
            - NODE_ENV=development
        working_dir: /usr/src/app
